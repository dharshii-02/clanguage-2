int main()
{
    int total_number_of_shelves;
    scanf("%d", &total_number_of_shelves);
    
    int total_number_of_queries;
    scanf("%d", &total_number_of_queries);
    
    while (total_number_of_queries--) {
        int type_of_query;
        scanf("%d", &type_of_query);
        
        if (type_of_query == 1) {int x, y;
            scanf("%d %d", &x, &y);

          

        // 1. Increment the book count for shelf x
        total_number_of_books[x]++;

        // 2. Reallocate the memory for shelf x to accommodate the new book
        // We use total_number_of_books[x] to determine the new size
        total_number_of_pages[x] = (int*)realloc(total_number_of_pages[x], total_number_of_books[x] * sizeof(int));

        // 3. Insert the book (y pages) at the end of the shelf
        // The index for the last book is [total_number_of_books[x] - 1]
        total_number_of_pages[x][total_number_of_books[x] - 1] = y;

             
             
           

